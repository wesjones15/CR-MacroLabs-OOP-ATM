package com.zipcodewilmington;

public class Investments extends Account {
    public Investments(Double balance, Integer userId, Integer accountId) {
        super(balance, userId, accountId);
    }
}
